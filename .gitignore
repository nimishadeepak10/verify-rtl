# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Simulation / local runs
build/
work_verify/
*.vvp
*.vcd
xvlog.log
xvlog.pb

# Formal-tab attempt log (local, not shared — see formal_log.py)
logs/

# Regression baselines (local, rebuildable by re-running verification --
# see regression.py)
regressions/

# SymbiYosys run-output workdirs (rebuildable from the .sby configs + RTL --
# model dumps, SMT2, solver logs, VCD/.yw traces, status.sqlite). Keep the
# .sby configs and source RTL, not the run output.
MESI_Formal/mesi_line/
MESI_Formal/mesi_line_cover/
MESI_Formal/mesi_multi_cache/
MESI_Formal/mesi_multi_cache_cover/

# IDE / OS
.idea/
.vscode/
.claude/
*.swp
.DS_Store
Thumbs.db

# Secrets
.env
*.pem
